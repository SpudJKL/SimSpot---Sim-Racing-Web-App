import { SimSpotValidators } from './sim-spot-validators';

describe('SimSpotValidators', () => {
  it('should create an instance', () => {
    expect(new SimSpotValidators()).toBeTruthy();
  });
});
